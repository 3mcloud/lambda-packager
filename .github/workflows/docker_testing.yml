name: Docker Publish Test Images CI
# Publish Images to DockerHub with a '-<git_commit_sha>' tag ending
# on every branch push, excluding 'master'.

on:
  push:
    branches:
      - "*"
      - "!master"

jobs:
  build-python36:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test python 3.6
      id: build_test
      run: make test VERSION=3.6

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "python-3.6-,python-3.6-qa"
        context: python/.
        dockerfile: python/3.6/Dockerfile

  build-python37:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test python 3.7
      id: build_test
      run: make test VERSION=3.7

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "python-3.7-,python-3.7-qa"
        context: python/.
        dockerfile: python/3.7/Dockerfile

  build-python38:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test python 3.8
      id: build_test
      run: make test VERSION=3.8

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "python-3.8-,python-3.8-qa"
        context: python/.
        dockerfile: python/3.8/Dockerfile

  build-python39:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test python 3.9
      id: build_test
      run: make test VERSION=3.9

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "python-3.9-,python-3.9-qa"
        context: python/.
        dockerfile: python/3.9/Dockerfile

  build-node12x:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test node 12.x
      id: build_test
      run: make test VERSION=12.22 RUNTIME=node

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "node-12.22-,node-12.22-qa"
        context: node/.
        dockerfile: node/12.22/Dockerfile

  build-node14x:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v2

    - name: Build and test node 14.x
      id: build_test
      run: make test VERSION=14.18 RUNTIME=node

    - name: push
      if: steps.build_test.outcome == 'success'
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: 3mcloud/lambda-packager
        username: ${{ secrets.DOCKERHUBUSER }}
        password: ${{ secrets.DOCKERHUBPASS }}
        tags: "node-14.18-,node-14.18-qa"
        context: node/.
        dockerfile: node/14.18/Dockerfile
